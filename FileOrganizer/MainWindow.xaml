<Window x:Class="FileOrganizer.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modern File Organizer" Height="500" Width="650" Background="#1E1E2E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="txtFolderPath" Width="480" Height="35" VerticalContentAlignment="Center" Padding="8,0" Background="#2A2A3C" Foreground="White" BorderBrush="#444" IsReadOnly="True"/>
            <Button x:Name="btnSelectFolder" Content="Select Folder" Width="110" Height="35" Margin="10,0,0,0" Background="#3B82F6" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="BtnSelectFolder_Click"/>
        </StackPanel>

        <Button x:Name="btnOrganize" Grid.Row="1" Content="Organize Files" Height="45" Background="#10B981" Foreground="White" FontWeight="Bold" Cursor="Hand" Click="BtnOrganize_Click" Margin="0,0,0,15"/>

        <TextBox x:Name="txtLog" Grid.Row="2" IsReadOnly="True" Background="#11111B" Foreground="#00FF66" FontFamily="Consolas" FontSize="13" Padding="10" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
